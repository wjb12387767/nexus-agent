# Nexus Agent - 环境变量示例
# 复制为 .env 并填入你的凭据
# 完整变量列表见 docs/environment-variables.md

# ── 模型 API 凭据 ──
# 填入你的 API key（任选一个 provider）
# GROK_API_KEY=sk-your-grok-key
# OPENAI_API_KEY=sk-your-openai-key
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key
# DEEPSEEK_API_KEY=sk-your-deepseek-key

# ── 自定义 API 中继端点 ──
# 如果你使用本地中继服务（如 one-api、new-api），取消注释并填入
# OPENAI_BASE_URL=http://127.0.0.1:8080/v1

# ── 外部服务集成（可选） ──
# Docling Serve API（透明文档转换，read 工具处理 PDF/Office 时优先调用）
# 需先运行: pip install docling-serve && python -m docling_serve.run
# DOCLING_SERVICE_URL=http://localhost:5001

# ── 调试与性能 ──
# 启用时序日志
# PI_TIMING=1

# ── MCP 配置 ──
# MCP server 配置在 ~/.nexus/agent/mcp.json，不在此文件
# 参见 docs/mcp-config.md
